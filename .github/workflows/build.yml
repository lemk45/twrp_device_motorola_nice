name: Compilar TWRP

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Baixar os arquivos do próprio repositório
      uses: actions/checkout@v4

    - name: Configurar Ambiente Linux
      run: |
        sudo apt-get update
        sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git gnupg gperf imagemagick lib32readline-dev lib32z1-dev libelf-dev liblz4-tool libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev python3 python3-pip

    - name: Instalar a ferramenta Repo do Google
      run: |
        mkdir -p ~/bin
        curl https://googlesource.com | base64 --decode > ~/bin/repo
        chmod a+x ~/bin/repo
        sudo ln -sf ~/bin/repo /usr/bin/repo

    - name: Sincronizar Código-Fonte do TWRP
      run: |
        # Cria a pasta de build em paralelo para não misturar com o checkout inicial
        mkdir -p ../twrp_base
        cd ../twrp_base
        git config --global user.name "Seu Nome"
        git config --global user.email "seu_email@exemplo.com"
        repo init --depth=1 -u https://github.com -b twrp-12.1
        repo sync -j$(nproc) --force-sync

    - name: Mover Árvore de Dispositivos para o Local Correto
      run: |
        # Cria a pasta de destino dentro da estrutura do código-fonte
        mkdir -p ../twrp_base/device/motorola/nice
        # Copia todos os arquivos que você enviou para o GitHub (exceto a pasta oculta .github)
        rsync -arv --exclude='.github' --exclude='.git' ./ ../twrp_base/device/motorola/nice/

    - name: Executar a Compilação (.img)
      run: |
        cd ../twrp_base
        export ALLOW_MISSING_DEPENDENCIES=true
        source build/envsetup.sh
        lunch twrp_nice-eng 
        make recoveryimage -j$(nproc)

    - name: Salvar o arquivo .img gerado
      uses: actions/upload-artifact@v4
      with:
        name: TWRP-Nice-Final
        path: ../twrp_base/out/target/product/nice/recovery.img
